# v2.1.24 → v2.2.0 upgrade (intent-based re-apply)

Generated: 2026-08-16
Base (merge-base): `aecad86` (upstream v2.1.24, 2026-07-02)
HEAD at generation: `ffa20a6`
Upstream target: `upstream/main` (v2.2.0)

Method: `/migrate-nanoclaw` — clean upstream in a worktree, re-apply the 12
fork commits on top, validate, swap. NOT a merge (462 upstream commits, heavy
churn on our hotspot files, and two customizations need re-evaluation against
breaking changes). Customization details: [../docs/CUSTOMIZATIONS.md](../docs/CUSTOMIZATIONS.md).
Path decisions (user-confirmed 2026-08-16): intent-based migrate; keep local
container build (not the hardened prebuilt image, for now).

## ✅ Cutover record (completed 2026-08-16)

- **Swapped:** main reset to `2e91be09` (upstream `cde0e39` v2.2.0 + reapply). Backup
  branch/tag `pre-migrate-bbd91967-20260816-120631`. DB+sessions backup at
  `~/nanoclaw-backups/pre-v2.2-20260816-122030`.
- **Validation:** host build + 1442 host tests, container typecheck + 212 container
  tests (incl. 2 reply-enforcement tests) — all green.
- **OneCLI gateway:** 1.36.0 → 1.41.0 in `~/.onecli` (docker compose). Persisted
  `ONECLI_BIND_HOST=172.17.0.1` in `~/.onecli/.env` (was inline-only — a plain
  `compose up` would have rebound to 127.0.0.1 and cut container credential access).
  Verified `/v1/health` 200 host-side AND from a container. CLI pin unchanged (2.2.5).
- **Memory:** each active group's `CLAUDE.local.md` copied to `instructions.prepend.md`
  (the file the new composer reads; `CLAUDE.local.md` is no longer read). Originals
  kept as inert backups. Persona composition verified at runtime — `persona.md`
  imported first. Deeper distillation into the `memory/` OKF tree is deferred
  (optional; standing instructions carry full behavior; host auto-scaffolds `memory/`).
- **Scheduled tasks:** no central-DB task state; legacy tasks live in chat sessions and
  keep firing (observed: ai-news-daily briefings, game-gecko scripts). The MCP→`ncl
  tasks` tool-surface change is handled by the image rebuild. Nothing to migrate.
- **Image:** rebuilt local `nanoclaw-agent-v2-61d30892:latest` (poppler verified;
  one-door + `ncl tasks` MCP surface). Egress lockdown left OFF (host-local backends).
- **Stamp + restart:** upgrade-state stamped `2.2.0`/`migrate-nanoclaw`; service restarted;
  3 central-DB migrations applied; Slack socket connected; no tripwire, no new errors.
- **Not run autonomously:** live Slack round-trip (send any message to an agent — exercises
  👀 indicator + reply enforcement); optional PostHog telemetry; `git push` to fork remote
  (would be a force-push — history was rewritten).
- **Pre-existing (unrelated):** vaulted Anthropic subscription hit a session/usage limit
  earlier in the day (account quota, not the migration).

## Reapply order (in the worktree)

1. **Slack skill** — re-run `/add-slack` apply against the worktree: `git fetch
   origin channels`, `git show origin/channels:src/channels/slack.ts > <wt>/src/channels/slack.ts`
   (+ `slack-registration.test.ts`), append `import './slack.js';` to
   `src/channels/index.ts`, `pnpm install @chat-adapter/slack@4.29.0`. Slack now
   also brings the `slack-formatting` container skill (moved to the `channels`
   branch upstream) — the add-slack apply handles it.
2. **👀 reaction indicator** (CUSTOMIZATIONS §1) — re-apply the fork block at the
   bottom of `src/channels/slack.ts` AFTER step 1 (it overrides the bridge's
   `setTyping`). Needs `reactions:write` (already granted).
3. **Inbound attachment size cap** (§2) — `src/channels/chat-sdk-bridge.ts`,
   `MAX_ATTACHMENT_SIZE` from `MAX_MEDIA_SIZE` env (default 50MB), checked pre-
   and post-download. **Conflict hotspot** (5 upstream commits) — read the new
   file first, place the checks where downloads happen now.
4. **poppler-utils** (§2) — one line in `container/Dockerfile` apt-get list.
5. **NO_PROXY exemption** (§3) — `src/container-runner.ts`, inject
   `NO_PROXY=host.docker.internal,localhost,127.0.0.1` after the OneCLI apply
   block. **Conflict hotspot** (14 upstream commits) + load-bearing for the daily
   news brief (RSSBrew :8001), movie-recs (Qdrant :6333), RSSHub :1200, Ollama
   :11434. Keep the do-not-remove warning comment.
6. **Reply enforcement** (§7) — `container/agent-runner/src/poll-loop.ts`.
   **RE-IMPLEMENT, do not diff-apply** (9 upstream commits reshaped this file).
   First check whether upstream now guarantees a user-facing reply on chat turns;
   if so, drop. Otherwise re-implement `batchHasUserChat` + `pendingUserReply`
   against the new `processQuery` structure. Port `poll-loop.test.ts` too.
7. **x-integration skill** (§8) — copy `.claude/skills/x-integration/` from the
   main tree (dropped upstream in v2).
8. **fork-sync CI** (§6) — copy `.github/workflows/fork-sync-skills.yml` from
   main tree. Fork-only file, no upstream equivalent.
9. **.gitignore fork additions** (§8) — `_bmad/`, `.claude/skills/bmad-*/`.
10. **Agent model = opus** (§5) — no code; already in `container_configs` DB. No
    worktree action.

Behavior/content files (CLAUDE.md, groups/*) are NOT touched by the code
upgrade — copied/left as-is per skill principle (data dirs untouched).

## Breaking-change interaction matrix (10 upstream `[BREAKING]` entries)

| # | Breaking change | Applies? | Action |
|---|-----------------|----------|--------|
| 1 | Agent templates → Agent Plugins 1.0.0 | Low | We provision via `ncl` manually, not templates. Verify no local template in use; re-fetch from registry if so. |
| 2 | Host lifecycle registry (onShutdown moved) | **No** | Our host modules are pristine upstream — the `response-registry`/`onShutdown` imports are all unmodified core files. Upgrade handles them. |
| 3 | Hardened agent image recommended | Deferred | Keep local build (custom Dockerfile: poppler). Revisit hardened image later. Add `agent-image` key to `versions.json` from upstream for parity. |
| 4 | iMessage unification | No | Not used. |
| 5 | Provider-agnostic OKF memory; persona → `instructions.prepend.md` | **Yes** | Per-group: check for legacy memory, run `/migrate-memory` where present. Groups currently have no `memory/` dir; scaffold auto-creates on boot. |
| 6 | Channel installs = single source of truth (setup rework) | Indirect | We re-run `/add-slack` anyway (step 1). No extra action. |
| 7 | `slack-formatting`/`whatsapp-formatting` → channels branch | **Yes** | Handled by re-running `/add-slack` (step 1). |
| 8 | Scheduled tasks MCP → `ncl tasks` | **Yes (verify)** | Our install carries the scheduling MCP tool (`.claude-fragments/module-scheduling.md`). Audit each group for live schedules; migrate per `docs/ncl-tasks-migration.md`. No host cron/timers found → any schedules are in-app. |
| 9 | Task delivery one-door (`send_message` needs `to`) | **Yes** | Rebuild image; clear/compact existing sessions; update any custom instructions omitting `to`. Distinct from our reply-enforcement patch (§7). |
| 10 | Chat SDK pinned `4.29.0` | Aligned | Already 4.29.0; re-run `/add-slack` (step 1) satisfies it. |
| 11 | OneCLI SDK 0.5→2.2.1 needs `/v1` API | **Yes** | Gateway pin moved 1.36.0→1.41.0. Upgrade the running gateway per `docs/onecli-upgrades.md` BEFORE restart, or every credentialed call 404s. |

## Runtime migrations (post-swap, before/at restart)

- **OneCLI gateway** 1.36.0 → 1.41.0 (+ CLI 2.2.5, already matched). Follow
  `docs/onecli-upgrades.md`. Our `versions.json` also lacks the `agent-image`
  key present upstream — add it.
- **Memory**: per active group, run `/migrate-memory` if legacy memory exists.
- **Scheduled tasks**: audit + migrate to `ncl tasks` (`docs/ncl-tasks-migration.md`).
- **Sessions**: clear/compact existing sessions (one-door delivery requirement).
- **Upgrade marker**: `pnpm exec tsx scripts/upgrade-state.ts set "" migrate-nanoclaw`
  after build succeeds — the v2.1+ startup tripwire halts the host without it.

## Active agent groups to verify after cutover

`slack_ai-news-daily` (daily brief — NO_PROXY/RSSBrew critical), `slack_gpt-researcher`,
`slack_movie-recs` (Qdrant), `slack_book-recs`, `slack_hash-monkey`,
`slack_game-gecko`, `slack_project-tracker`, `slack_main`, `main`.

## Rollback

Backup branch + tag created at Phase 2.1 (`backup/pre-migrate-<hash>-<ts>` /
`pre-migrate-<hash>-<ts>`). `git reset --hard <tag>`, revert `versions.json` +
OneCLI gateway if it was upgraded, restart.
