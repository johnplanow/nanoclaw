# v2.2.0 → v2.3.0 upgrade (intent-based re-apply)

Generated: 2026-09-08
Base (merge-base): `cde0e396` (upstream v2.2.0, 2026-08-13)
HEAD at generation: `87450cfc` (fork main; 25 fork commits on the base)
Upstream target: tag `v2.3.0` = `54d9d9a5` (2026-08-24). NOT upstream/main tip:
main carries an unreleased `[BREAKING]` project-doc rewrite
(`claude-md-compose` → `project-doc-compose`) that we take with the next round.

Method: `/migrate-nanoclaw` — clean `v2.3.0` in `.upgrade-worktree`, re-apply
every fork block on top, validate (host build + vitest, container typecheck +
bun test), swap `main` to the worktree commit, deploy to **VM 206** (the live
host since 2026-09-07; aliera is the soak copy). Customization inventory:
[../docs/CUSTOMIZATIONS.md](../docs/CUSTOMIZATIONS.md). Path decisions
(user-confirmed 2026-09-08): update this guide from the manifest (no
re-extraction); validate on aliera then deploy to 206 (no live worktree test);
stay on classic Slack.

Safety net: aliera branch+tag `pre-migrate-87450cfc-20260908-134332`
(pushed); 206 tag `pre-migrate-206-3d1a11f5-20260908-134340`; 206 data
snapshot `~/nanoclaw-backups/pre-v2.3-20260908-134343/` (staging + all
`.claude-shared` + live `data/`, 206 MB).

## Scope

255 upstream commits, 538 upstream-changed files; 25 fork commits, 81 fork-changed
files; 17 files changed on both sides (the hotspots below). Tier 3.

## Reapply order (done in the worktree, in this order)

1. **Fork-only files copied as-is:** `.github/workflows/fork-sync-skills.yml`,
   `.claude/skills/x-integration/`, `scripts/backup-snapshot.ts`,
   `.nanoclaw-migrations/`, `docs/CUSTOMIZATIONS.md`, `src/tavily-manifest.test.ts`.
   `.gitignore` fork lines appended (`_bmad/`, `.claude/skills/bmad-*/`,
   `tools/clidash/clidash.config.json`).
2. **Pins:** `container/cli-tools.json` claude-code 2.1.238→2.1.251 + add
   `mcp-remote@0.1.38` (Tavily bridge); `container/agent-runner/package.json`
   `@anthropic-ai/claude-agent-sdk` ^0.3.238→`0.3.251` exact; `bun install`
   (bun.lock). `container/Dockerfile`: `poppler-utils` in the apt list (§2).
3. **clidash** (`/add-clidash`): `cp -R .claude/skills/add-clidash/add/tools/clidash
   tools/clidash` (identical to what we ran on v2.2.0).
4. **Slack** (`/add-slack` apply): 10 files from `upstream/channels` **at
   `c3f900f4`** (see CUSTOMIZATIONS §1 for why not HEAD), barrel imports
   `./slack.js` + `./slack-a2a-guard.js`, `pnpm install @chat-adapter/slack@4.29.0`.
   Then the 👀 block (§1) re-injected inside `createSlackBridge()` before the
   final `return Object.assign(bridge, …)`, with `log`/`ChannelSetup`/
   `InboundMessage` imports and the two constants.
5. **Attachment size cap** (§2): `src/channels/chat-sdk-bridge.ts` — constant
   after the imports, pre/post-download checks around `att.fetchData()`.
6. **NO_PROXY** (§3): NEW HOME `src/gateway-providers/onecli.ts` `contribute()`
   (the driver seam moved env assembly out of container-runner.ts).
7. **Tasks §9/§10:** `src/mailbox/types.ts` + `src/mailbox/sqlite/index.ts`
   (`latestFiredTaskSeries()`), `src/modules/scheduling/create.ts`
   (`resolveHostSession`, `sessionId` option), `src/cli/resources/tasks.ts`
   (`selectedSessions` union, `firedSeriesInSession`, `--in-origin-session` /
   `--session`, help text, `append-log` derivation), `src/delivery.ts`
   (`task_log` from chat sessions; origin-thread inheritance via
   `mailbox.getTask` + `msg.threadId`). All async / awaited.
8. **Agent-runner §7 + §12 + FULL-content nudge:** `poll-loop.ts` (constants,
   config fields, `lastWorkAt`/`lastEventAt`, `batchHasUserChat`, reply
   enforcement with `turnDelivered`, idle stream end, `QueryResult.lastEventAt`),
   `index.ts` (`process.exit(0)` after the loop).
9. **Tests ported:** `poll-loop.test.ts` (reply enforcement, idle lifecycle —
   new param positions), `tasks.test.ts` ("tasks inside a chat session"),
   `delivery.test.ts` (task_log from a chat session).

Not reapplied (no longer applicable): the container-runner.ts NO_PROXY block
(moved, item 6); `container/skills/slack-formatting/SKILL.md` local copy
(re-fetched by add-slack).

## Breaking-change interaction matrix (v2.3.0 `[BREAKING]` entries)

| # | Breaking change | Applies? | Action |
|---|---|---|---|
| 1 | New Slack experience / `/migrate-slack-agents` | Decision | **Stay on classic.** No ack needed outside `/update-nanoclaw`'s transaction; classic `instance='slack'` rows untouched. |
| 2 | Agent mailbox seam (host+runner registries; `DeliveryActionHandler` lost db arg; awaited writes) | **Yes** | §9/§10 re-implemented on the seam (item 7); runner `db/messages-in.ts` still wraps the mailbox so the poll-loop fork blocks port 1:1. |
| 3 | Session driver seam (group folder grammar; `ncl-…` container names; moved helpers) | **Yes** | `detect-driver-migration.ts`: only our old container-runner block flagged (superseded) + false positive on `groups/.git` (nested repo, not in DB). **Tooling:** anything matching `nanoclaw-v2-*` must switch to `docker ps --filter label=nanoclaw-session`. |
| 4 | Node ≥ 22 | OK | 206 and aliera on v22.23.2. |
| 5 | Central DB async behind `DbDriver` | **Yes** | All fork host code awaited (item 7). Startup still auto-runs migrations (`runMigrations(..., { mode: 'auto' })`). |
| — | Host restarts adopt running sessions | Behaviour | Restart no longer kills in-flight agents; use `ncl groups restart` to roll containers deliberately. |
| — | `--user uid:gid` + `HOME=/home/node` on non-root hosts | Behaviour | 206 runs uid 1000; unchanged effectively. |
| — | OneCLI 1.41.0 for new installs | Aligned | Already 1.41.0. |

## Runtime steps on 206 (after the swap + push)

1. `git fetch origin && git reset --hard origin/main` (history rewritten — not a ff).
2. `pnpm install --frozen-lockfile && pnpm run build`.
3. `./container/build.sh` (base image), then rebuild the two per-group images
   (hash-monkey, game-gecko) — `buildAgentGroupImage` via a one-off tsx script
   or `ncl groups restart --rebuild` after the host is up.
4. `pnpm exec tsx scripts/upgrade-state.ts set "" migrate-nanoclaw` (tripwire).
5. `systemctl --user restart nanoclaw` → central migrations apply at boot; watch
   the journal for `NanoClaw running` + Slack socket connect.
6. Roll containers: `ncl groups restart --id <each>` (new `ncl-…` names).
7. Acceptance: one turn per agent (hash-monkey / movie-recs / ai-news / game-gecko),
   👀 reaction, `ncl tasks list` shows watchers + legacy tasks, watcher fires,
   idle exit at 15 min (`Container exited … code=0`), 02:45 staging + dead-men.

## Rollback

`git reset --hard pre-migrate-206-3d1a11f5-20260908-134340` on 206 (aliera:
`pre-migrate-87450cfc-20260908-134332`), `pnpm install && pnpm run build`,
rebuild image, `upgrade-state.ts set`, restart. If the v2.3.0 host already
ran central-DB migrations 022+ against `data/v2.db`, restore `data/` from
`~/nanoclaw-backups/pre-v2.3-20260908-134343/data-live/` first.
